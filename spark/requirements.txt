# Spark Python job dependencies
# These are installed directly inside the Spark containers via docker-compose command.
# They are also the reference versions for local development.
pyspark==3.4.1
shap==0.44.0
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
matplotlib==3.8.2
pyarrow==14.0.1
